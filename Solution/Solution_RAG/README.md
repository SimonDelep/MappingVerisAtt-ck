# RAG local (retrieval / local_llm)

Pipeline hors-ligne : embeddings `sentence-transformers` + sélection par
similarité (`retrieval`) ou LLM local (`local_llm`).

Pour la variante **LLM cloud Together.ai**, voir le dossier frère :

`Solution/Solution_RAG_Together/`
